import { Typography } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const About = () => {
  return (
    <Box>
      <Typography>I AM ABOUT</Typography>
    </Box>
  );
};

export default About;
